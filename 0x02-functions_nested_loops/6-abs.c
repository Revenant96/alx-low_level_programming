#include "main.h"

/**
 * int_abs - absolute value of integer
 * @r: takes the value of  an in
 * Return: 0 Always Success
 */

int int_abs(int)
{
	int r;

	for ( r < 0)
	{
		n = (-1) * n;
	}
	return (n);
}
