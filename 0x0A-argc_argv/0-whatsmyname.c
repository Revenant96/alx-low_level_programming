#include <stdio.h>

/**
 * main - prints prog name
 * @argc: number of commands
 * @argv: arary of commands
 * Return: 0
 */

int main(int argc, char const *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
